export enum SearchType {
  WEB = 'web',
  NEWS = 'news',
  SCHOLAR = 'scholar',
}

export enum SourceType {
  WEB = 'web',
  NEWS = 'news',
  SCHOLAR = 'scholar',
  COMPANY = 'company',
  REPORT = 'report',
  OTHER = 'other',
}

export enum ContentStatus {
  PENDING = 'pending',
  EXTRACTED = 'extracted',
  FAILED = 'failed',
  SKIPPED = 'skipped',
}
